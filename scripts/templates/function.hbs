// This file is auto-generated.

import { Static{{pascalCaseEndpoint}}Parameters } from './parameters';
import { {{pascalCaseEndpoint}}Response } from './response';
import * as got from 'got';

/**
 * {{endpoint.description}}
 */
export function {{camelCaseEndpoint}}(args: Static{{pascalCaseEndpoint}}Parameters): Promise<{{pascalCaseEndpoint}}Response> {
  return got
    .post('https://slack.com/api/{{endpoint.url}}', {
      headers: {
        'Content-Type': 'application/x-www-form-urlencoded'
      },
      body: args
    })
    .then((response) => {
      return response.body;
    });
}
